{ ... }:

{
  # Bash and other terminal utilities
  programs.bash = {
    enable = true;
  };

  programs.atuin = {
    enable = true;
  };

  programs.zoxide = {
    enable = true;
  };

  programs.fzf = {
    enable = true;
  };
}
